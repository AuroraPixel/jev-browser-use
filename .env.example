# Set this in the invoking shell or MCP server environment. Never commit a real key.
TYPESAFE_API_KEY=
TYPESAFE_MODEL=jev-latest
# Optional HTTP(S) proxy, no SOCKS URL:
# TYPESAFE_PROXY=http://127.0.0.1:10808
# Optional isolated runtime state:
# JEV_BROWSER_USE_HOME=/absolute/path/to/runtime
