{
  "name": "jev-browser-use-extension",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "test": "vitest",
    "test:run": "vitest run",
    "typecheck": "wxt prepare && tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "wxt": "^0.20.0"
  },
  "private": true,
  "license": "MIT",
  "description": "Connect your existing Chrome profile to jev-browser-use.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AuroraPixel/jev-browser-use.git"
  }
}
